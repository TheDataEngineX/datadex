name: Enforce dev-to-main

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  enforce:
    uses: TheDataEngineX/.github/.github/workflows/enforce-dev-to-main.yml@main
    secrets: inherit
